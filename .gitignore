# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
dist/
.eggs/

# Data (large files not committed — run pipeline locally)
pipeline/data/
# Raw recipe corpora are never redistributed (see pipeline/ENRICHMENT.md §1)
pipeline/corpora/*/raw/
# pairings.db is a generated build artifact — do not commit.
# pairings.json is the owned canonical database — it IS committed.
web/public/pairings.db

# Node
node_modules/
web/dist/

# Misc
.DS_Store
*.log
.env
web/test/.deployed.json
