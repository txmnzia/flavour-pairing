# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
dist/
.eggs/

# Data (large files not committed — run pipeline locally)
pipeline/data/
# pairings.db is always a generated artifact — never commit.
web/public/pairings.db
# pairings.json: commit it when generated from real data (process.py).
# Do NOT commit it when it's the demo version (generate_demo.py).
# If unsure, check file size: real data is several MB, demo is ~300 KB.

# Node
node_modules/
web/dist/

# Misc
.DS_Store
*.log
.env
