# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
dist/
.eggs/

# Data (large files not committed — run pipeline locally)
pipeline/data/
# pairings.db is always a generated artifact — never commit.
web/public/pairings.db
# pairings.json is gitignored by default (demo version is generated at build time).
# To commit real data generated by process.py, use: git add -f web/public/pairings.json
web/public/pairings.json

# Node
node_modules/
web/dist/

# Misc
.DS_Store
*.log
.env
